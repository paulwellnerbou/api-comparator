{
  "git": {
    "commitMessage": "Release v${version}",
    "tagName": "v${version}",
    "requireCleanWorkingDir": true,
    "requireBranch": "main"
  },
  "github": {
    "release": true,
    "releaseName": "Release v${version}",
    "assets": ["dist/api-comparator", "dist/index.js", "templates/**/*", "package.json", "DISTRIBUTION.md", "LICENSE"]
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "before:release": "bun run build && bun run build:bin",
    "after:release": "echo 'Bumping to next dev version' && npm version prerelease --preid=dev --no-git-tag-version && git add package.json && git commit -m 'Bump to next dev version [skip ci]' && git push"
  }
}
